import React from 'react';
import { Box, List, ListItem } from '@chakra-ui/react';
import { Switch, Route, Link } from 'react-router-dom';

export const Home = () => {
  //let { path, url } = useRouteMatch();
  return <Box fontSize="xl">Home</Box>;
};
